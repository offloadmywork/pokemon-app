import { describe, it } from 'vitest';

// Challenge Tower API tests (scaffold)
// Expected endpoints/methods (to be implemented in client.js or a dedicated module):
// - getTowerState()
// - startTowerRun()
// - submitFloorResult(floor, result)
// - claimTowerReward(rewardId)
// - getTowerResetInfo()

describe('Challenge Tower API', () => {
  it.todo('fetches current tower state and progress');
  it.todo('starts a tower run for the current user');
  it.todo('submits a floor result and returns updated progress');
  it.todo('claims a tower reward by id');
  it.todo('returns weekly reset schedule info');
  it.todo('handles API errors gracefully');
});
